<h1>Editar categoria</h1>
<div class="card"
    <div class="card-body">
        <form action="/admin/categorias/edit" method="POST" class="d-flex flex-column justify-content-center align-items-center p-3">
        <input type="hidden" name="id" value="{{categoria._id}}">
            <label class="align-self-start p-1" for="nome">Nome: </label>
            <input type="text" name="nome" id="nome" placeholder="Nome da categoria" class="form-control" value="{{categoria.nome}}">
            <label class="align-self-start p-1 mt-3" for="slug" class="">Slug: </label>
            <input type="text" name="slug" id="slug" placeholder="Slug da categoria" class="form-control" value="{{categoria.slug}}">
            <button type="submit" class="btn btn-success mt-3 btn-block w-25">Editar Categoria</button>
        </form>
    </div>
</div>